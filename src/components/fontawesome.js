// import the library
import { library } from '@fortawesome/fontawesome-svg-core';

import { faHome, faUser, faCode, faPaperPlane, faBlog } from '@fortawesome/free-solid-svg-icons'

library.add(
    faHome,
    faUser,
    faCode,
    faPaperPlane,
    faBlog
  // more icons go here
);